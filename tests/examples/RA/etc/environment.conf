#!/bin/bash

export HOME_DIR=/usr/local/etc/RA

export DEVICE_SCANNER="hpaio:/net/HP_LaserJet_3055?ip=10.1.1.6" # Obtain device name from scanimage -L

export SMTP_HOST=mail.t-unwired.com
export SMTP_PORT=25
